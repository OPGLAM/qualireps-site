/** Site-wide constants. */

export const SITE_NAME = 'QualiReps';
export const SITE_TAGLINE = 'A strength ledger for daily practice.';
export const SITE_ORIGIN = 'https://qualireps.app';

/** The installable web-app origin users open to install the PWA. */
export const APP_URL = 'https://qualireps.app';

/** Public contact for legal matters (footer + legal pages). */
export const LEGAL_EMAIL = 'legal@qualireps.app';

/** Price shown across the site. One purchase, lifetime access. */
export const PRICE_LABEL = '9,99 €';

/** Default Open Graph image (single brand image for V1 — see README). */
export const OG_IMAGE = '/og-default.svg';

/** Primary navigation (EN). */
export const NAV = [
  { href: '/', label: 'Home' },
  { href: '/forms', label: 'Forms' },
  { href: '/pricing', label: 'Pricing' },
];
